-- the game properties file

return {
    -- game name
    name = "template",

    -- what version of love to use
    loveVersion = "11.3",

    -- specific constant game variables
    game = {

        -- the internal game dimensions
        width = 512, height = 512,

        -- where we can put paths that we reference elsewhere if needed
        paths = {

        }
    }
}